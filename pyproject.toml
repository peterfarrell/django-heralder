[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "django-heralder"
authors = [{name = "Jason Blum", email = "jason.blum@gmail.com"}]
dynamic = ["version", "description"]
